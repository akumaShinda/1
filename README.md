# QuantumTrade Backend (FastAPI)

This is the backend API for QuantumTrade — a fully AI-powered crypto trading platform.

## How to run

```bash
pip install -r requirements.txt
uvicorn main:app --reload
